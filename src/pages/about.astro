---
import { getEntry } from "astro:content";
import ContentPage from "../layouts/ContentPage.astro";

const entry = await getEntry("pages", "about");
if (!entry) throw new Error("Content not found: pages/about");
---

<ContentPage entry={entry} />
